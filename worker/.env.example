# ============================================================
# Namnesis Worker — Cloudflare Worker 环境变量
# 非敏感变量在 wrangler.toml [vars] 中配置
# 敏感变量通过 `wrangler secret put <NAME>` 设置
# ============================================================

# ---- R2 存储凭证（通过 wrangler secret put 设置）----
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key

# ---- wrangler.toml [vars] 中的非敏感配置 ----
# R2_ACCOUNT_ID=your-cloudflare-account-id
# R2_BUCKET_NAME=namnesis-capsules
# SOUL_TOKEN_ADDRESS=0x...（部署合约后填入）
# SOUL_GUARD_ADDRESS=0x...（部署合约后填入）
# BASE_SEPOLIA_RPC=https://sepolia.base.org
